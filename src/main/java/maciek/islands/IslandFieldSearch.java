package maciek.islands;

/**
 * It searches particular field of given island that represents the island (e.g. bottom-right field).
 */
public interface IslandFieldSearch {

	Field search(Field anyIslandField);

}
